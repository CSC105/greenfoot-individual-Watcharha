import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class co2 here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class co2 extends Actor
{
    /**
     * Act - do whatever the co2 wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        
    }  
    public co2(){
        GreenfootImage image = getImage();  
        image.scale(400, 300);
        setImage(image);
    }
}
